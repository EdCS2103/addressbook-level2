package seedu.addressbook.data.person;

/**
 * A read only imutable interface for objects which can be converted into a printable string representaion
 */

public interface Printable {

	String getPrintableString();
}
